version: '3.8'

services:
  solr:
    image: solr:9.4
    container_name: ai_tools_solr
    ports:
      - "8983:8983"
    volumes:
      - solr_data:/var/solr
    command: solr-precreate ai_tools_opinions
    environment:
      SOLR_HEAP: "1g"

volumes:
  solr_data:
